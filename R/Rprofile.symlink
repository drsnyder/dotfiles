require(ggplot2)
require(reshape2)
require(stats)
require(data.table)
#require(plyr)
require(dplyr)
require(psych) # describeBy
options(menu.graphics=FALSE)

wideScreen <- function(howWide=Sys.getenv("COLUMNS")) {
  options(width=as.integer(howWide))
}
